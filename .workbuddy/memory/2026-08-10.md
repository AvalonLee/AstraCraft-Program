# 2026-08-10 工作记录

## SkillMall 仓库建设与 CRLF 坑修复

- 完成首批三件套收录：superpowers（📦 vendored, MIT, obra/superpowers, HEAD 44c9b2d6）、
  agent-skills-spec（🔗 存根, CC-BY-4.0, 主动指向 live spec）、anthropics-office-skills
  （🔗 存根, source-available, C 级）。
- 关键修复：`.gitattributes` 原 `* text=auto eol=lf` 会把 vendored `src/` 在全新
  checkout 时归一化为 LF，导致 `upstream.lock` 的 content_hash（按原始 CRLF 字节算）
  失配、`vendor.py --verify` 在 Linux CI runner 必然失败。
  - 修复：对 `**/src/**` 设 `-text`（git 不触碰换行符），仓库存上游原始字节；
    并 `git add --renormalize` 让 superpowers/src 的 blob 回到 CRLF。
  - 验证方法：用 `git worktree add <tmp> HEAD` 模拟全新 checkout，跑 `vendor.py --verify`
    （注意 `git clone` 本地路径在本沙箱被拦，用 worktree 替代）。本地 clone 失败是沙箱限制，非仓库问题。
- 三个 commit：fc550d7（初始化）、63555b1（CRLF 修复）、22b7ce4（文档更正）。共 238 文件。
- validate.py 对 license_tier=A 但 mode=link-only 仅告警不报错（agent-skills-spec 属主动选择，可接受）。
