# 2026-08-13 工作记录

## SkillMall 目录结构重组（九大分类收纳 + 文档归位）

- **九大用途分类统一移入 `entries/` 大目录**：writing-docs / dev-engineering /
  design-creative / data-analytics / research-intel / ops-automation /
  business-office / agent-infra / meta-skillcraft
- **文档类文件统一移入 `docs/`**：CONTRIBUTING / CODE_OF_CONDUCT / CHANGELOG /
  THIRD_PARTY_NOTICES（与原有 4 份规范文档同处）
- **根目录仅保留核心入口**：README / INDEX / LICENSE / LICENSE-CODE / scripts /
  _template / .github
- 脚本适配：
  - `scripts/_common.py`：`discover_entries()` 改扫 `root/entries/<分类>/<条目>`；
    `NOTICES_PATH` / `CHANGELOG_PATH` 指向 `docs/` 下
  - `scripts/gen_index.py`：HEADER 编辑路径提示、分类分组标题改为 `entries/<分类>/`
  - `scripts/validate.py`：`check_categories_exist()` 改查 `REPO_ROOT/entries/<分类>`
  - `scripts/vendor.py`：docstring 示例路径更新
- 配置适配：
  - `.gitignore`：superpowers 白名单改为 `!entries/meta-skillcraft/superpowers/src/`
  - `.gitattributes`：linguist-vendored 规则 `*/*/src/**` → `**/src/**`（深度泛化）
- 文档更新：README（结构图 / 分类表 / 使用与贡献命令）、docs/CONTRIBUTING、
  docs/vendoring-guide、docs/THIRD_PARTY_NOTICES、PR 模板、Issue 模板、
  docs/CHANGELOG 追加结构记录
- 验证：`validate.py` 通过（仅 agent-skills-spec 既有的主动 link-only 告警）、
  `gen_index.py --check` 通过、`vendor.py --verify` 通过（superpowers 180 文件
  content_hash 未变，移动未破坏快照）
- 经验：
  - Windows 本地跑 validate.py 需先 `$env:PYTHONIOENCODING='utf-8'`，否则 GBK 控制台
    打印 emoji（🔗/📦）报 UnicodeEncodeError
  - 本机 Python 用 `C:\SDWebuiaki\python\python.exe`（3.10.11，已装 pyyaml/jsonschema）；
    `py` launcher 未配置默认版本
  - 沙箱中 `.git` 目录只读，git 写操作（mv/add/commit）需升级权限；文件系统层移动
    用原生 `Move-Item` 即可

## SkillMall 收录：TencentDB Agent Memory（agent-infra，MIT vendored）

- 条目：`entries/agent-infra/tencentdb-agent-memory`，kind framework
- 上游：TencentCloud/TencentDB-Agent-Memory，默认分支 `feat/server_team` @ commit 4dca55c（2026-08-11）
- 协议：MIT（A 级绿灯），copyright_holder = Tencent；根 LICENSE 与 package.json license:MIT 一致
- 快照：src/ 830 文件 / 12.7 MB（剔除 9 个超大资源：2 个演示视频 .mov + 6 张 >1MB 流程图 + 上游 .github/workflows；见 src/assets/FETCH.md）
- 评级：standard（未实测，admission.checked 不含 H5；需 Docker 三件套 + LLM 配置，非即插即用 skill）
- 风险备注：默认分支为迭代中的 feat/server_team；README 安装段 clone URL 笔误（Tencent 应为 TencentCloud）
- 收录方式：git clone 被网络中断，改用 codeload tarball + 手动构建 src/ 与 upstream.lock（content_hash 用 _common.compute_content_hash 计算）
- 提交：837dfd5（834 文件）；validate.py / gen_index.py --check / vendor.py --verify 通过；已推送 origin/main

## SkillMall 设定调整：从「快照式收录」改为「轻量 SKILL.md 索引」（2026-08-13）

- **不再收录上游源码快照**：删除全部 `src/`（superpowers 180 文件 + tencentdb 830 文件）、
  `upstream.lock`、`vendor.py`、`docs/vendoring-guide.md`；`.gitignore` / `.gitattributes`
  移除 src 相关规则
- **新形态**：每个条目只保留一个 `entries/<分类>/<id>/SKILL.md`（frontmatter 元数据 +
  介绍 + 安装指令），Agent 读取后即可快速定位并安装对应 skill 项目
- **脚本重写**：`_common.py`（frontmatter 解析 / discover_entries）、`validate.py`
  （frontmatter schema + id/category 一致性 + 安装指令完整性 + 去重 + 已移除名单）、
  `gen_index.py`（从 frontmatter 生成，链接指向 SKILL.md，badge 用 ★/⚠️）、
  `schema/meta.schema.json` 精简（去掉 vendoring/license_tier/copyright_holder）
- **docs 重写**：admission-criteria（轻量 A1-A4）、license-policy（仅标注，无再分发义务）、
  writing-skill-md.md（新，替代 vendoring-guide）、CONTRIBUTING（三步）、
  THIRD_PARTY_NOTICES（导航声明）、skill-spec-cheatsheet（映射节改为 frontmatter）
- **4 个条目全部转换**为 SKILL.md：superpowers（★ core）、tencentdb-agent-memory（⚠️）、
  agent-skills-spec（⚠️）、anthropics-office-skills（⚠️）
- **坑**：frontmatter 的 `summary_en` 含冒号+空格必须加引号（如 "skills: docx..."），
  否则 YAML 解析失败、validate 报"必填字段缺失"

## 分支策略设定：dev 为默认开发分支（2026-08-13）

- GitHub 仓库默认分支已通过 API 改为 `dev`（AvalonLee 凭据，PATCH /repos/AvalonLee/SkillMall）
- 本地工作分支切到 dev；以后所有修改都推送到 dev
- 合并入 main 需等待用户明确命令
- 分支流程说明已写入 docs/CONTRIBUTING.md
