<!--
实测笔记 —— 本仓库的核心资产。

这份文件的价值在于「你真的用过」。它是 SkillMall 区别于书签列表的唯一理由。
admission.tier 为 core 的条目，CI 会检查本文件非空且不等于模板原文。

写作要求：
  · 第一人称，写你实际遇到的情况
  · 允许有主观判断，但要给出依据
  · 就事论事评价项目本身，不对作者作价值判断（见 CODE_OF_CONDUCT.md）
  · 所有关于上游的补充说明都写在这里，不要去改 src/ 里的任何文件
-->

# 实测笔记

**实测日期**：2026-08-10
**实测环境**：Windows 11 / Git Bash / Python 3.13 / Claude Code v?.?.?
**实测版本**：见 `upstream.lock` 的 `commit`

## 跑通了什么

具体描述你做了什么测试。用了哪个功能、输入是什么、输出是什么。
如果有截图或输出片段，贴关键部分。

## 踩到的坑

- 装的时候遇到什么问题，怎么解决的
- 文档没写清楚但实际很重要的前提条件
- 在 Windows / 中文环境下的额外问题（这类信息上游通常不会写）

## 和同类的对比

| 维度 | 本项目 | 同类 A | 同类 B |
|---|---|---|---|
| 上手成本 | | | |
| 覆盖范围 | | | |
| 维护活跃度 | | | |

一段话说明：什么情况下选它，什么情况下选别的。

## 我的判断

值不值得用、推荐给谁、不推荐给谁。

如果 `admission.tier` 评了 `core`，在这里说明理由（对应哪几项加分项）。

## 上游补充说明

<!--
零修改 vendoring 意味着你不能去改 src/ 里的文件。
如果上游代码有 bug、有需要注意的地方、或者你想给出修改建议，写在这里。

例如：
  · src/scripts/setup.sh 第 12 行假定了 bash 5+，macOS 自带 bash 3.2 会失败，
    需要先 brew install bash
  · SKILL.md 的 description 触发词偏窄，实际使用时建议手动指定
-->

暂无。
